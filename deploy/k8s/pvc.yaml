apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mcp-monitor-data
  namespace: mcp-monitor
  labels:
    app.kubernetes.io/name: mcp-monitor
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
